name: macos-runner-podman

on:
  workflow_dispatch:

jobs:
  ci:
    runs-on: macos-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Install podman
        run: |
          brew install podman
          podman -version